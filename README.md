# Parkinson’s ML App – Final Release

This version includes full code.

## ▶ How to run
```bash
pip install -r requirements.txt
python model_pipeline.py   # trains models and generates artifacts
streamlit run streamlit_app.py
```
